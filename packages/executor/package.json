{
  "name": "@humanhands/executor",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic browser action executor — orchestrates Playwright actions through semantic selectors",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    }
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --dts --clean",
    "dev": "tsup src/index.ts --format esm,cjs --dts --watch",
    "type-check": "tsc --noEmit",
    "lint": "eslint src/",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@humanhands/shared-types": "workspace:*",
    "@humanhands/selector-engine": "workspace:*",
    "@humanhands/event-system": "workspace:*",
    "playwright": "^1.48.2"
  },
  "devDependencies": {
    "tsup": "^8.3.5",
    "typescript": "^5.6.3",
    "@types/node": "^22.9.0"
  }
}
